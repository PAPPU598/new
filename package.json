{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "Daily Expense Tracker with Dark Mode (INR)",
  "author": "Pappu Sarkar",
  "license": "MIT",
  "private": true,

  "scripts": {
    "start": "npx cap serve",
    "build": "echo Build step not required",
    "android": "npx cap open android"
  },

  "dependencies": {
    "@capacitor/core": "^5.0.0"
  },

  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  }
}
